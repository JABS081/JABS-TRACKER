import {adminClient,json,requireUser,fail} from '../_supabase.js';
export default async function handler(req,res){try{if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});const {user,profile}=await requireUser(req);const db=adminClient();let q=db.from('subscriptions').select('*').order('created_at',{ascending:false}).limit(1);q=profile.company_id?q.eq('company_id',profile.company_id):q.eq('user_id',user.id);const {data,error}=await q.maybeSingle();if(error)throw error;return json(res,200,{data:data||{plan_id:'FREE',status:'active',asset_limit:1}})}catch(e){return fail(res,e)}}
