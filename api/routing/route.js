export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const {start,destination,waypoints}=req.query;const base=process.env.ROUTING_URL;if(!base)return res.status(503).json({error:'Routing provider is not configured.'});if(!start||!destination)return res.status(400).json({error:'start and destination are required.'});try{const url=new URL(base);url.searchParams.set('start',start);url.searchParams.set('destination',destination);if(waypoints)url.searchParams.set('waypoints',waypoints);const r=await fetch(url);const body=await r.json();return res.status(r.status).json(body)}catch(e){return res.status(502).json({error:'Routing provider unavailable.'})}}
