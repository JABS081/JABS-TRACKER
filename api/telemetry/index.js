import crypto from 'node:crypto';
import {adminClient,json,fail} from '../_supabase.js';
const num=v=>Number.isFinite(Number(v));
export default async function handler(req,res){try{if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});const key=req.headers['x-jabs-device-key'];if(!key)return json(res,401,{error:'Device credential required.'});const body=req.body||{};const {device_id,asset_id,asset_type,latitude,longitude,speed=0,heading=0,accuracy=null,timestamp}=body;if(!device_id||!asset_id||asset_type!=='PHONE'||!num(latitude)||!num(longitude))return json(res,400,{error:'device_id, asset_id, asset_type PHONE, latitude and longitude are required.'});if(Number(latitude)<-90||Number(latitude)>90||Number(longitude)<-180||Number(longitude)>180)return json(res,400,{error:'Invalid coordinates.'});const db=adminClient();const hash=crypto.createHash('sha256').update(String(key)).digest('hex');const {data:device,error:de}=await db.from('devices').select('id,asset_id,company_id,active,revoked_at,credential_hash').eq('device_code',device_id).maybeSingle();if(de)throw de;if(!device||!device.active||device.revoked_at||device.credential_hash!==hash)return json(res,401,{error:'Invalid or revoked device credential.'});if(device.asset_id!==asset_id)return json(res,403,{error:'Device is not authorized for this asset.'});const recorded_at=timestamp||new Date().toISOString();const loc={device_id:device.id,asset_id,latitude:Number(latitude),longitude:Number(longitude),speed:Number(speed)||0,heading:Number(heading)||0,accuracy:num(accuracy)?Number(accuracy):null,recorded_at};const {error:le}=await db.from('locations').insert(loc);if(le)throw le;const {error:ae}=await db.from('assets').update({latitude:loc.latitude,longitude:loc.longitude,speed:loc.speed,heading:loc.heading,status:loc.speed>3?'MOVING':'STOPPED',last_updated:recorded_at,updated_at:new Date().toISOString()}).eq('id',asset_id);if(ae)throw ae;return json(res,201,{ok:true,asset_id,recorded_at})}catch(e){return fail(res,e)}}
